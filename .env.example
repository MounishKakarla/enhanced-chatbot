# Example environment variables for Enhanced SQL Chatbot
# Copy this file to `.env` and fill in your real secrets.

# Groq API
GROQ_API_KEY=your-groq-api-key-here

# Admin credentials
ADMIN_USER=your-admin-username
ADMIN_PASSWORD=your-admin-password

# Database settings
# Supported DB types: mysql, postgres, sqlite
DB_TYPE=mysql
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_HOST=localhost
DB_NAME=your-database-name

# PostgreSQL settings (if using postgres)
PG_USER=postgres
PG_PASSWORD=1234
PG_HOST=localhost
PG_PORT=5432
PG_NAME=your-database-name

# Optional: other app settings
# APP_DEBUG=true
